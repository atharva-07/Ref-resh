const fallbackHeading: string = "No Stories! :(";
const fallbackContent: string = "You have not posted any stories yet.";

const StoryGrid = () => {
  return <div>Story Grid</div>;
};

export default StoryGrid;
