type Post implements TimeStamps {
  _id: ID!
  content: String!
  images: [String!]
  likes: [User!]
  commentsCount: Int!
  bookmarks: [String!]
  author: User!
  createdAt: String!
  updatedAt: String!
}

input PostData {
  content: String!
  images: [String!]
}

type PostEdge {
  node: Post!
  cursor: String!
}

type PageInfo {
  hasNextPage: Boolean!
  endCursor: String
}

type Feed {
  edges: [PostEdge!]!
  pageInfo: PageInfo!
}
