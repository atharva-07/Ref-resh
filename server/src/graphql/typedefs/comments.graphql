type Comment implements TimeStamps {
  _id: ID!
  content: String!
  commenter: User!
  post: ID!
  likes: [User!]
  parentComment: Comment
  topLevelComment: Comment
  createdAt: String!
  updatedAt: String!
}
